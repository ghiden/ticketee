<strong>New Comment</strong>
<%= form_for [@ticket, @comment] do |f| %>
  <%= render "shared/error_messages", :object => f.object %>
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <%= f.submit %>
<% end %>
